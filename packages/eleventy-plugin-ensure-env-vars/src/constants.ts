export const DEBUG_PREFIX = '11ty-plugin:ensure-env-vars'

export const ERROR_MESSAGE_PREFIX = {
  invalidConfiguration:
    '[🕵️ 11ty-plugin-ensure-env-vars] INVALID CONFIGURATION',
  invalidEnvironment: '[🕵️ 11ty-plugin-ensure-env-vars] INVALID ENVIRONMENT'
}
